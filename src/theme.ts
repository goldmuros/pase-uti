import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#00CED1", // Turquoise
    },
    secondary: {
      main: "#B2DFDB", // Light turquoise for secondary
    },
  },
});

export default theme;
