import type { Cultivos } from "../types/Cultivos";
import type { Pase } from "../types/Pase";

const DetallePaciente = () => {
	const obtenerListaPases = () => {
		return [] as Pase[];
	};

	const obtenerListaCultivos = () => {
		return [] as Cultivos[];
	};

	return <>DetallePaciente</>;
};

export default DetallePaciente;
